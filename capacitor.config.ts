import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'philipp.endrulat.musicplayer',
  appName: 'MusicPlayer',
  webDir: 'www'
};

export default config;
